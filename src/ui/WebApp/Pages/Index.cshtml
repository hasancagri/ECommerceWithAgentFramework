@page
@model IndexModel
@{
    ViewData["Title"] = "Ana Sayfa";
}

<div class="container mt-5">
    <div class="row row-cols-1 row-cols-md-3 row-cols-lg-4 g-4">


        @if (!Model.Products.Any())
        {
            <div class="col">
                <div class="alert alert-warning" role="alert">
                    Ürün bulunamadı!
                </div>
            </div>
            return;
        }

        @* 011 US2: kart, Tüm Ürünler ile ortak partial'dan çizilir (FR-008). *@
        @foreach (var product in Model.Products!)
        {
            <partial name="_ProductCard" model="product"/>
        }
    </div>

    @* 011 US2: dashboard ilk 8 ürünü gösterir; tamamı Tüm Ürünler ekranında (FR-007). *@
    <div class="text-center mt-4">
        <a asp-page="/Products/Index" class="btn btn-outline-primary">Tüm Ürünleri Gör</a>
    </div>
</div>